



node {
    checkout scm
    // Load the file 'pipelineTrace.groovy' from the current directory, into a variable called "externalMethod".
    def trace = load("pipelineTrace.groovy")

    stage('Build') {
        trace.startStage("${env.WORKSPACE}", "${env.BUILD_NUMBER}",  "${env.GIT_BRANCH }")
//        buildLog.startStage('Build')
    }

    stage('Code Coverage') {
//        buildLog.startStage('Code Coverage')
    }
    stage('Deploy') {
 //       buildLog.startStage('Deploy')
    }




}