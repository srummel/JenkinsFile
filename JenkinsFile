import com.Zot

node {
    try {
        stage('Build') {
            echo 'Build'
			def z = new com.Zot()
			z.checkOutFrom("Shawn")
        }
        stage('Unit Testing') {
            echo 'Unit Testing'
        }
	
        stage('Code Coverage') {
            echo 'Code Coverage'
        }

        stage('Code Analysis') {
            echo 'Code Analysis'
        }

        stage('Creating install package') {
            echo 'Creating install package'
        }

        stage('Artifact Upload') {
            echo 'Artifact Upload'
        }

        stage('Deployment to DEV environment') {
            echo 'Deployment to DEV environment'
        }

        stage('Automated Acceptance testing') {
            echo 'Automated Acceptance testing'
        }

        stage('Combine Logs') {
            echo 'Combine Logs'
        }

        stage('Build Summary') {
            echo 'Build Summary'
        }

        stage('Clean up workspace') {
            echo 'Clean up workspace'
        }

        stage('Prepare code') {
            echo 'Prepare code'
        }

        stage('Testing') {
            echo 'Testing'
            echo 'Testing - publish coverage results'
        }

        stage('Staging') {
            echo 'Deploy Stage'
        }
        stage('Deploy') {
            echo 'Deploy - Backend'
            echo 'Deploy - Frontend'
        }
		
  } catch (e) {
    // If there was an exception thrown, the build failed
    currentBuild.result = "FAILED"
    throw e
  } finally {
  }
}